LLM_PROVIDER=groq
LLM_MODEL=openai/gpt-oss-120b
# One or more keys, comma-separated. Extra keys are rotated to automatically
# when a key hits its rate limit (per-minute or per-day).
GROQ_API_KEYS=your_groq_api_key_here,optional_second_key,optional_third_key
